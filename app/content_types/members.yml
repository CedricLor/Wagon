# Human readable name of this type
name: Members

# Lowercase, underscored handle used to access this type
slug: members

# Explanatory text displayed in the back-office
description: A description of the content type for the editors

# Slug of field used to identify entries by default, such as the title
label_field_name: full_name

# Valid values: manually, created_at, updated_at, or the slug of any field
order_by: manually

# Valid values: asc (ascending) and desc (descending). Set to asc by default.
# order_direction: asc

# Specify a field slug to group entries by that field in the back-office.
# group_by: <your field>

# Activate public 'create' API (e.g for a contact form)
# public_submission_enabled: false

# Array of emails to be notified of new entries made with the public API
# public_submission_accounts: ['john@example.com']

# Control the display of the content type in the back-office.
# display_settings:
#   seo: false              # display the SEO tab for the content entries
#   advanced: false         # display the Advanced tab for the content entries
#   position: 1             # position in the sidebar menu
#   hidden: false           # hidden for authors?

# By default, the back-office displays the _label property (see label_field_name) of the content entry. This can be modified by writing your own Liquid template below:
# entry_template: '<a href="{{ link }}">{{ entry._label }}</a>' # The default template

# A list describing each field
fields:
- full_name: # The lowercase, underscored name of the field
    label: Nom complet # Human readable name of the field
    type: string
    required: true
    localized: false

- description: # The lowercase, underscored name of the field
    label: Description # Human readable name of the field
    type: text
    required: true
    hint: Insérez ici la biographie de ce membre
    localized: true
    text_formatting: html # html (uses rich text editor), markdown or text (uses plain text editor)

- category: # The lowercase, underscored name of the field
    label: Catégorie # Human readable name of the field
    type: select
    required: true
    hint: La catégorie à laquelle vous appartenez permet de vous classer sur la page A propos
    localized: false    # if localized, use
    #   en: ['option1_en', 'option2_en']
    #   fr: ['option1_fr', 'option2_fr']
    select_options: ['Executive', 'Board Member', 'Contributor']

- member_index_photo:
  label: Photo du membre
  hint: Une photo du membre qui sera affiché sur la page A propos
  type: file
  required: false
  localization: false

- news:
    label: News
    hint: Les actualités publiées par ce membre
    type: has_many
    class_name: news
    inverse_of: member
    ui_enabled: true

- photos:
    label: Photos
    hint: Les photos que vous avez publié (dans la section photo)
    type: has_many
    class_name: photos
    inverse_of: member
    ui_enabled: true

- events:
    label: Events
    hint: Les évènements que vous avez créés
    type: has_many
    class_name: events
    inverse_of: member
    ui_enabled: true
